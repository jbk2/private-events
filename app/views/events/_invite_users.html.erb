<h2>Not yet invited users:</h2>
<%= form_with model: @event, url: invite_users_event_path(@event), method: :post do |f| %>
  <%= f.collection_select :attendee_id, @not_yet_invited_users, :id, :email, {}, { multiple: true } %>
  <%= f.submit 'Invite Users'%>
<% end %>